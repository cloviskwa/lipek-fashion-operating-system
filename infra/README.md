\
# infra

Docker Compose definitions for local Postgres/Redis/OpenSearch/asset-storage-emulator (`docker/`, `compose/`), and deployment manifests/IaC (`deployment/`) once `ADR-0009` (hosting) resolves. First real content lands in `FOUND-014`/`FOUND-018`. Never commit production secrets here — see `docs/implementation/environment-variables.md`.
