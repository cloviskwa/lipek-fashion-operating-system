# Admin API Extensions

**Status:** Derived (skeleton — populated as each plugin ships its Admin API extensions, starting `SEC-001`/`CONTENT-001`)
**Source:** `docs/internal/LIPEK_MASTER_TECHNICAL_SOURCE_OF_TRUTH.md` §3.2, §20A

Nothing to document yet. This file becomes the index of every Admin API extension each plugin's colocated `dashboard/` extension depends on, one section per owning plugin (`lipek-security`, `lipek-content`, `tailoring`, `alterations`, `laundry`, `appointments`, `crm`, `loyalty`, `documents`, `customer-experience`, `analytics-events`, `integrations` — each at `apps/server/src/plugins/<name>/`), updated as part of every task that adds one.
