# Performance Budget

**Status:** Derived (skeleton — thresholds set in `COM-011`, Phase 3)
**Source:** `docs/internal/LIPEK_MASTER_TECHNICAL_SOURCE_OF_TRUTH.md` §41, §50, §51

Target: the platform must work smoothly "on a low-cost mobile device and average internet connection" (source of truth §50). Core Web Vitals budgets (LCP/INP/CLS) and Lighthouse CI thresholds are defined and enforced starting `COM-011`; re-validated at production scale in `OPS-012`. No budget is set yet — nothing exists to measure.
