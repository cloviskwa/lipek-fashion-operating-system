\
# packages/graphql

Generated GraphQL types/documents from the Vendure Shop and Admin schemas (codegen output — never hand-written). First generated once `apps/server` exists (`FOUND-014`) and codegen tooling is selected (`FOUND-013` dependency plan §1).
