# packages/schemas

Shared Zod (or equivalent) validation contracts — form payloads, MFA enrollment/challenge shapes, webhook payloads, AI tool input/output shapes. First real content lands alongside `SEC-001` (auth payload schemas).
