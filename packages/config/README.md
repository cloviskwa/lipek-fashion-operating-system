\
# packages/config

Shared ESLint/TypeScript/Prettier configuration so every app enforces the same standard (strict TS, accessibility lint rules). Populated in `FOUND-015` from the baseline configs currently sitting in `_reference/legacy-prototype/` (`eslint.config.mjs`, `tsconfig.json`, `.prettierrc` at repo root).
