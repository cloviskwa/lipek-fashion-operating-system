\
# apps/storefront

Not yet scaffolded. Bootstrapped by task `FOUND-014` via `@vendure/create`'s official Next.js storefront starter (current: Next.js 16 / React 19 — verified as of this repo's Phase 0 audit; re-verify at bootstrap time per `AGENTS.md`).

Reusable assets from the prior prototype (`_reference/legacy-prototype/`) are ported in deliberately during `FOUND-020` — routes/IA, SEO/JSON-LD pattern, design tokens (via `packages/ui`) — never copied in wholesale. This app is a renderer of backend-owned data (source of truth §3.1), never a system of record.

See `docs/architecture/TARGET_REPOSITORY_STRUCTURE.md` §2 for the full responsibility matrix.
