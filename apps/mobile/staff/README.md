# apps/mobile/staff

Placeholder only. Per `ADR-0007`, staff/admin use is expected to be served by the responsive Vendure Dashboard (validated on tablets in `MOBILE-004`, Phase 11) rather than a dedicated native app, unless a concrete on-the-go admin requirement emerges. This directory exists so the decision has a home if reversed — it is not scheduled work.
