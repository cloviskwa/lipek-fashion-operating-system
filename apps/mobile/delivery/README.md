# apps/mobile/delivery

Not yet scaffolded. Native (React Native/Expo) delivery/courier app per `ADR-0007`, built in Phase 6 (`MOBILE-003`) once laundry pickup/delivery scheduling (`LAUNDRY-004`) exists to consume. Needs reliable background location and push, and a camera-heavy proof-of-delivery flow — reasons it is native rather than a Capacitor wrapper.
